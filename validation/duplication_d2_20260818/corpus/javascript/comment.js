function comment(source) {
  // comment 1
  const alpha = source + 1;  // trailing 1
  // comment 2
  const beta = source + 2;  // trailing 2
  // comment 3
  const gamma = source + 3;  // trailing 3
  // comment 4
  const delta = source + 4;  // trailing 4
  // comment 5
  const epsilon = source + 5;  // trailing 5
  // comment 6
  const zeta = source + 6;  // trailing 6
  // comment 7
  const eta = source + 7;  // trailing 7
  // comment 8
  const theta = source + 8;  // trailing 8
  // comment 9
  const iota = source + 9;  // trailing 9
}
