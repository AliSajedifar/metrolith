class FixtureReordered {
  static void reordered(int source) {
    int beta = source + 2;
    int alpha = source + 1;
    int gamma = source + 3;
    int delta = source + 4;
    int epsilon = source + 5;
    int zeta = source + 6;
    int eta = source + 7;
    int theta = source + 8;
    int iota = source + 9;
  }
}
