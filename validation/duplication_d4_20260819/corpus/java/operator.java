class FixtureOperator {
  static void operator(int source) {
    int alpha = source - 1;
    int beta = source + 2;
    int gamma = source + 3;
    int delta = source + 4;
    int epsilon = source + 5;
    int zeta = source + 6;
    int eta = source + 7;
    int theta = source + 8;
    int iota = source + 9;
  }
}
